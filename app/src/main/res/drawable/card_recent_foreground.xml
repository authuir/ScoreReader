<?xml version="1.0" encoding="utf-8"?>
<!--
    Foreground overlay drawn on top of the card content. In the default
    state it is invisible; when the card receives DPAD focus or is pressed
    we paint a thick accent-coloured border (and a faint white wash) so
    the highlighted card is obvious from a couch viewing distance.

    Corner radius MUST match `app:cardCornerRadius` on the MaterialCardView
    so the border hugs the card outline.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#33FFFFFF" />
            <stroke android:width="3dp" android:color="@color/accent" />
            <corners android:radius="8dp" />
        </shape>
    </item>

    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="#22FFFFFF" />
            <stroke android:width="3dp" android:color="@color/accent" />
            <corners android:radius="8dp" />
        </shape>
    </item>

    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="#22FFFFFF" />
            <stroke android:width="3dp" android:color="@color/accent" />
            <corners android:radius="8dp" />
        </shape>
    </item>

    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="8dp" />
        </shape>
    </item>

</selector>
